---
description: java 编码规范
globs: ["**/*.java"]
alwaysApply: false
---

## Java 代码规范

### 枚举值格式要求
- **每个枚举值必须独占一行**，不允许多个枚举值在同一行
- 枚举值后用逗号结尾，并增加一个 **//** 形式的尾行注释说明用途
- 相关的枚举值之间可以用空行分组，增强可读性
- 分号单独独占一行作为枚举项的结束标识

### 强制要求
- 创建或修改 Java 类时，AI必须遵循此格式
- 发现不符合格式的类时，应主动提示是否需要重构